# Validate Footnotes
1. Read all markdown files in the specified group directory
2. For each file, extract every footnoted citation
3. Cross-reference against source PDFs in the same directory
4. Produce a Checkup_<group>.md with: issue summary at top, per-paper results, accuracy stats
5. Stage, commit, and push when done
NEVER enter plan mode. Execute directly.
